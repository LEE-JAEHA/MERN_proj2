import React from 'react';
import {BrowserRoutera as Router} from 'react-router-dom';
// ES

// 라우터를 사용하는 앱 일부 감싸기 

const App =() => {
  return <Router>
    
  </Router>
  
  ;
}

export default App;
